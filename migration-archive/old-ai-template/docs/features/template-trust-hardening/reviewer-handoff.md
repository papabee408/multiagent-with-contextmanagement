# Reviewer Handoff

> Generated by `scripts/sync-handoffs.sh`. Edit `plan.md` and rerun sync instead of hand-editing this file.

## Source Digest
- brief-sha: 2c4efa470bd867dece419fb52b6fa2e2630cc2ceb3dd46daaf81622d0a35bb9a
- plan-sha: a757e487dead25086b37266abd2d701a719f04b7cea78c831dbb799d6dd0962a
- project-context-sha: ecca12d92cc35ce868121156749e28d9e88d062a0da9d755e5b9c498b7d845ac
- architecture-sha: 1c26799c1924e637673f787dcdc39b9629ed412be609d7e917ce0b668dd92be1
- gates-sha: 1adab9c1e10189cf3208ed88d74b904eb34756d1ea719b90b7e4d493396e1b86
- sync-script-sha: c172b022ffdb8e59d019416f5c7995c3b9e72faf2c1bcc7f7b7202bc129ac28c
- generated-at-utc: 2026-03-27 07:07:22Z

## Focus
- workflow mode: full
- execution mode: single
- regression hotspots: scripts/feature-packet.sh, scripts/gates/_validation_cache.sh, scripts/gates/run.sh, scripts/start-feature.sh, tests/gate-cache.test.sh, tests/start-feature.test.sh
- approval target: current final diff plus the current approval-target hash; reviewer `PASS` must match the final state that reaches gate completion
- architecture / reuse focus: bootstrap entrypoints stay in `scripts/`; validation-cache behavior stays in `scripts/gates/_validation_cache.sh`; regression coverage stays in the existing shell smoke tests under `tests/`.; current mode/token validation flow in `scripts/feature-packet.sh`, fingerprint helpers in `_validation_cache.sh`, and the existing smoke-test fixture patterns in `tests/start-feature.test.sh` and `tests/gate-cache.test.sh`.; only extract a small local prevalidation helper in `scripts/feature-packet.sh` if it keeps the bootstrap path clearer; otherwise keep changes inline and minimal.
- scope drift watchpoints: unrelated workflow docs, role contracts, and gate scripts outside the reviewed bootstrap/cache/setup-check paths; feature packet schema changes beyond the minimum required for these trust fixes; new workflow modes, new role types, or broad caching redesign

## Quality Checklist
- reuse / componentization: current mode/token validation flow in `scripts/feature-packet.sh`, fingerprint helpers in `_validation_cache.sh`, and the existing smoke-test fixture patterns in `tests/start-feature.test.sh` and `tests/gate-cache.test.sh`.; only extract a small local prevalidation helper in `scripts/feature-packet.sh` if it keeps the bootstrap path clearer; otherwise keep changes inline and minimal.
- hardcoding / config centralization: accepted risk/workflow/execution mode tokens, the setup stamp path, and any file lists that define cache invalidation coverage.; cache input file lists and setup-stamp handling should stay in one place rather than being redefined across scripts/tests.; the reviewed script/test filenames and existing mode tokens already required by the template contract.
- performance / waste watchpoints: bootstrap entrypoints stay in `scripts/`; validation-cache behavior stays in `scripts/gates/_validation_cache.sh`; regression coverage stays in the existing shell smoke tests under `tests/`.; keep shell-only behavior with no new runtime dependencies; do not change packet layout or gate command shapes; preserve compatibility with current `scripts/gates/run.sh` and `scripts/start-feature.sh` entrypoints.; avoid repeated expensive transforms/lookups or duplicated render/update paths inside touched files

## Acceptance
- fail conditions: target files drift outside `plan.md` scope, dependency constraints are violated, reuse/componentization opportunities are ignored, centralized config is bypassed, or obvious performance waste is introduced
- approval binding: reviewer receipt must record the current approval-target hash; any later approval-target change invalidates the reviewer approval automatically

## Manual Notes
- Edit `plan.md` and rerun `scripts/sync-handoffs.sh` if generated defaults need to change.
