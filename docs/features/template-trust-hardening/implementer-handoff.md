# Implementer Handoff

> Generated by `scripts/sync-handoffs.sh`. Edit `plan.md` and rerun sync instead of hand-editing this file.

## Source Digest
- brief-sha: 2c4efa470bd867dece419fb52b6fa2e2630cc2ceb3dd46daaf81622d0a35bb9a
- plan-sha: a757e487dead25086b37266abd2d701a719f04b7cea78c831dbb799d6dd0962a
- project-context-sha: ecca12d92cc35ce868121156749e28d9e88d062a0da9d755e5b9c498b7d845ac
- architecture-sha: 1c26799c1924e637673f787dcdc39b9629ed412be609d7e917ce0b668dd92be1
- gates-sha: 1adab9c1e10189cf3208ed88d74b904eb34756d1ea719b90b7e4d493396e1b86
- sync-script-sha: c172b022ffdb8e59d019416f5c7995c3b9e72faf2c1bcc7f7b7202bc129ac28c
- generated-at-utc: 2026-03-27 07:07:21Z

## Scope
- rq_covered: RQ-001, RQ-002, RQ-003
- workflow mode: full
- execution mode: single
- target files: scripts/feature-packet.sh, scripts/gates/_validation_cache.sh, scripts/gates/run.sh, scripts/start-feature.sh, tests/gate-cache.test.sh, tests/start-feature.test.sh
- task order: Task 1 -> Task 2 -> Task 3
- out-of-scope reminders: unrelated workflow docs, role contracts, and gate scripts outside the reviewed bootstrap/cache/setup-check paths; feature packet schema changes beyond the minimum required for these trust fixes; new workflow modes, new role types, or broad caching redesign

## Constraints
- implementer mode: serial
- merge owner: implementer
- parallel worker packages: serial mode
- architecture placement: bootstrap entrypoints stay in `scripts/`; validation-cache behavior stays in `scripts/gates/_validation_cache.sh`; regression coverage stays in the existing shell smoke tests under `tests/`.
- dependency constraints: keep shell-only behavior with no new runtime dependencies; do not change packet layout or gate command shapes; preserve compatibility with current `scripts/gates/run.sh` and `scripts/start-feature.sh` entrypoints.
- reuse / config directives: current mode/token validation flow in `scripts/feature-packet.sh`, fingerprint helpers in `_validation_cache.sh`, and the existing smoke-test fixture patterns in `tests/start-feature.test.sh` and `tests/gate-cache.test.sh`.; only extract a small local prevalidation helper in `scripts/feature-packet.sh` if it keeps the bootstrap path clearer; otherwise keep changes inline and minimal.; cache input file lists and setup-stamp handling should stay in one place rather than being redefined across scripts/tests.; the reviewed script/test filenames and existing mode tokens already required by the template contract.

## Acceptance
- done when: invalid bootstrap inputs leave no new feature directory, no active-feature switch, and valid bootstrap flows still pass the existing smoke coverage.; unchanged inputs still reuse the PASS receipt, but committed infra test/runtime shell changes force `--reuse-if-valid` to rerun the gate instead of reusing stale PASS output.; repeated `start-feature.sh` calls continue to surface setup alerts until `scripts/check-project-setup.sh` succeeds, without breaking valid feature bootstrap.

## Manual Notes
- Edit `plan.md` or `brief.md` and rerun `scripts/sync-handoffs.sh` if generated defaults need to change.
