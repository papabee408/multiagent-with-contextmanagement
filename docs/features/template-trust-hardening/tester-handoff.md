# Tester Handoff

> Generated by `scripts/sync-handoffs.sh`. Edit `brief.md` or `plan.md` and rerun sync instead of hand-editing this file.

## Source Digest
- brief-sha: 2c4efa470bd867dece419fb52b6fa2e2630cc2ceb3dd46daaf81622d0a35bb9a
- plan-sha: a757e487dead25086b37266abd2d701a719f04b7cea78c831dbb799d6dd0962a
- project-context-sha: ecca12d92cc35ce868121156749e28d9e88d062a0da9d755e5b9c498b7d845ac
- architecture-sha: 1c26799c1924e637673f787dcdc39b9629ed412be609d7e917ce0b668dd92be1
- gates-sha: 1adab9c1e10189cf3208ed88d74b904eb34756d1ea719b90b7e4d493396e1b86
- sync-script-sha: c172b022ffdb8e59d019416f5c7995c3b9e72faf2c1bcc7f7b7202bc129ac28c
- generated-at-utc: 2026-03-27 07:07:22Z

## Coverage
- workflow mode: full
- execution mode: single
- rq coverage: `RQ-001`: `scripts/feature-packet.sh` must validate user-supplied bootstrap mode/risk inputs before creating a packet or changing `.context/active_feature`, so invalid bootstrap requests leave no partial packet behind.; `RQ-002`: `scripts/gates/run.sh --reuse-if-valid` must invalidate stale full-gate receipts when infra test files or other gate runtime shell scripts change, while still preserving legitimate cache reuse when inputs are unchanged.; `RQ-003`: `scripts/start-feature.sh` must keep surfacing project-setup alerts until the setup check passes, so users do not silently miss required GitHub/project configuration work.
- required scenarios: RQ-001 normal/error/boundary coverage; RQ-002 normal/error/boundary coverage; RQ-003 normal/error/boundary coverage
- priority risks: Keep the fix set narrow to the reviewed defects and their direct tests.; keep shell-only behavior with no new runtime dependencies; do not change packet layout or gate command shapes; preserve compatibility with current `scripts/gates/run.sh` and `scripts/start-feature.sh` entrypoints.; unrelated workflow docs, role contracts, and gate scripts outside the reviewed bootstrap/cache/setup-check paths; feature packet schema changes beyond the minimum required for these trust fixes; new workflow modes, new role types, or broad caching redesign

## Setup
- fixtures / mocks / setup: none beyond the existing shell, git, node, and sha tooling already assumed by the template.; existing shell helpers in `scripts/gates/_helpers.sh` and `_validation_cache.sh`, plus the current smoke-test harnesses in `tests/*.test.sh`.
- test edit policy: implementer owns baseline test updates; tester may strengthen `tests/**` only when coverage gaps remain after implementation
- execution notes / commands: Run `scripts/gates/check-tests.sh`, then update `test-matrix.md` with the concrete test files you executed.

## Acceptance
- matrix expectations: every brief RQ must have a `test-matrix.md` row with concrete normal/error/boundary coverage, test file, and `VERIFIED` status before tester returns `PASS`
- trivial-mode note: tester role remains active for this workflow mode

## Manual Notes
- Edit `brief.md` or `plan.md` and rerun `scripts/sync-handoffs.sh` if generated defaults need to change.
