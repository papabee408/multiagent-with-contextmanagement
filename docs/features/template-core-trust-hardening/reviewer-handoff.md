# Reviewer Handoff

> Generated by `scripts/sync-handoffs.sh`. Edit `plan.md` and rerun sync instead of hand-editing this file.

## Source Digest
- brief-sha: 771c6c064af72cd470159e4ad8206be76e3298f17da9eb24fff694a4bd1194b7
- plan-sha: 793877e25768dd56b73000417c8aa320848625a44ac86ed331538d9e062a77ec
- project-context-sha: ecca12d92cc35ce868121156749e28d9e88d062a0da9d755e5b9c498b7d845ac
- architecture-sha: 1c26799c1924e637673f787dcdc39b9629ed412be609d7e917ce0b668dd92be1
- gates-sha: 4b4eea6e5956c2140b90562f4d6ba37dc39603a24f6736cad5e78b94562ecd12
- sync-script-sha: 29b6b7210e885878bfb84df0568d5b3aa33caeeef3e26b5f7ec84e5655848ec8
- generated-at-utc: 2026-03-27 08:32:50Z

## Focus
- workflow mode: full
- execution mode: single
- regression hotspots: README.md, docs/agents/gate-checker.md, docs/agents/tester.md, docs/context/GATES.md, docs/features/README.md, scripts/_role_receipt_helpers.sh, scripts/_run_log_helpers.sh, scripts/gates/check-role-chain.sh, scripts/record-role-result.sh, scripts/start-feature.sh, scripts/sync-handoffs.sh, tests/gates.test.sh, tests/run-log-ops.test.sh, tests/start-feature.test.sh
- approval target: current final diff plus the current approval-target hash; reviewer `PASS` must match the final state that reaches gate completion
- architecture / reuse focus: keep receipt generation in `scripts/_role_receipt_helpers.sh` and `scripts/record-role-result.sh`, stale-verification enforcement in `scripts/gates/check-role-chain.sh`, bootstrap safety in `scripts/start-feature.sh`, and operator guidance in the existing workflow docs; `approval_target_hash`, `approval_target_files`, `write_role_receipt`, and the existing shell fixture helpers in `tests/gates.test.sh`; if multiple scripts need to know which roles require approval binding or run-log section boundaries, keep that rule in one helper path rather than duplicating conditionals
- scope drift watchpoints: `scripts/gates/run.sh`; `scripts/gates/check-tests.sh`; reviewer/security role contracts unless approval-binding wording requires a small consistency touch; unrelated feature packets or historical session logs

## Quality Checklist
- reuse / componentization: `approval_target_hash`, `approval_target_files`, `write_role_receipt`, and the existing shell fixture helpers in `tests/gates.test.sh`; if multiple scripts need to know which roles require approval binding or run-log section boundaries, keep that rule in one helper path rather than duplicating conditionals
- hardcoding / config centralization: approval-binding role rules, mode-specific packet file expectations, and active-feature safety checks should stay centralized in existing workflow helpers/docs instead of being duplicated ad hoc; approval-binding role lists and mode-specific packet file expectations; current role names, markdown section titles, and gate labels that are already part of the packet format
- performance / waste watchpoints: keep receipt generation in `scripts/_role_receipt_helpers.sh` and `scripts/record-role-result.sh`, stale-verification enforcement in `scripts/gates/check-role-chain.sh`, bootstrap safety in `scripts/start-feature.sh`, and operator guidance in the existing workflow docs; stay inside the existing shell-first workflow; do not add new runtime dependencies or split trust checks across unrelated scripts; avoid repeated expensive transforms/lookups or duplicated render/update paths inside touched files

## Acceptance
- fail conditions: target files drift outside `plan.md` scope, dependency constraints are violated, reuse/componentization opportunities are ignored, centralized config is bypassed, or obvious performance waste is introduced
- approval binding: reviewer receipt must record the current approval-target hash; any later approval-target change invalidates the reviewer approval automatically

## Manual Notes
- Edit `plan.md` and rerun `scripts/sync-handoffs.sh` if generated defaults need to change.
